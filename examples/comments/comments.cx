// comments.cx — M1 样例：行注释与（可嵌套）块注释
// 这是行注释：从 // 到行尾被忽略

/* 这是块注释：
   可以跨越多行 */

/* 块注释支持嵌套：
   /* 内层块注释 */
   外层继续 */

func main(): i32 {
    /* 函数体中的块注释 */
    var x: i32 = 1; // 行尾注释
    printf("%d\n", x);
    return 0;
}
