comptime func add(a: i32, b: i32): i32 {
    return a + b;
}

comptime var BASE: i32 = 100;
comptime var SUM = add(BASE, 23);

func main(): void {
    printf("%d\n", SUM);
    printf("%d\n", add(1, 2));
    var local = SUM + add(5, 5);
    printf("%d\n", local);
}
